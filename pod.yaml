# Generation of Kubernetes YAML is still under development!
#
# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-1.6.4
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2020-09-19T08:38:11Z"
  labels:
    app: playground
  name: playground
spec:
  containers:
  - command:
    - nginx
    - -g
    - daemon off;
    image: docker.io/library/nginx:latest
    name: nginx
    ports:
    - containerPort: 80
      hostPort: 8080
      protocol: TCP
    resources: {}
    securityContext:
      allowPrivilegeEscalation: true
      capabilities: {}
      privileged: false
      readOnlyRootFilesystem: false
    volumeMounts:
    - mountPath: /etc/nginx/conf.d:Z
      name: home-blue-podman-playground-nginx
    workingDir: /
  - command:
    - catalina.sh
    - run
    image: docker.io/library/tomcat:latest
    name: tomcat
    resources: {}
    securityContext:
      allowPrivilegeEscalation: true
      capabilities: {}
      privileged: false
      readOnlyRootFilesystem: false
    workingDir: /usr/local/tomcat
  - command:
    - flask
    - run
    - --host
    - 0.0.0.0
    env:
    - name: FLASK_DEBUG
      value: "1"
    - name: FLASK_ENV
      value: development
    - name: FLASK_APP
      value: app.py
    image: localhost/flask:latest
    name: flask
    resources: {}
    securityContext:
      allowPrivilegeEscalation: true
      capabilities: {}
      privileged: false
      readOnlyRootFilesystem: false
    volumeMounts:
    - mountPath: /app:Z
      name: home-blue-podman-playground-python-api-app
    workingDir: /app
  volumes:
  - hostPath:
      path: /home/blue/podman/playground/nginx
      type: Directory
    name: home-blue-podman-playground-nginx
  - hostPath:
      path: /home/blue/podman/playground/python-api/app
      type: Directory
    name: home-blue-podman-playground-python-api-app
status: {}

